import os

#  Input shape of the model
INPUT_SHAPE = (128, 128, 3)

#  Shape of the displayed image
MAIN_SHAPE = (300, 300, 3)

#  Path to the model
GENERATOR_PATH = os.sep.join(["model", "model.h5"])